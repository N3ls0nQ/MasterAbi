import useData from "../hooks/UserContext"

export const getOverallAveragePoints = (subjects) => {

    let count = 0;

    for (let i = 0; i < subjects.length; i++) {
        for(let n = 0; n < subjects[i].semesters.length; n++){
            
        }
    }
}